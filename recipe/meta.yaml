{% set name = "k3d" %}
{% set version = "2.7.1" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name|upper }}-{{ version }}.tar.gz
  sha256: 546166930e5231dddd61ac11a56ba1e6e0fcd21c7474a5a2e4604f62d9840ced

build:
  noarch: python
  number: 0
  script: "{{ PYTHON }} -m pip install . --no-deps -vv"

requirements:
  build:
    - setuptools
  host:
    - python
    - pip
  run:
    - python
    - ipywidgets >=7.0.1
    - ipydatawidgets
    - traittypes
    - traitlets
    - numpy >=1.11.0
test:
  commands:
    - cd {{ SRC_DIR }}/js && npm install && xvfb-run -a grunt test  # [linux]
  requires:
    - grunt
    - nodejs

about:
  home: http://github.com/K3D-tools/K3D-jupyter
  license: MIT
  license_family: MIT
  license_file: LICENSE.txt
  summary: 'Jupyter notebook extension for 3D visualization.'
  dev_url: https://github.com/K3D-tools/K3D-jupyter

extra:
  recipe-maintainers:
    - renefritze
    - tgandor
    - artur-trzesiok
    - marcinofulus
